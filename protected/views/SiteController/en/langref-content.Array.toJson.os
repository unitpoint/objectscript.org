return {
	ret = "mixed",
	retDesc = <<<END'
Преобразованное значение.
END,
	desc = <<<END"
Преобразует массив в совместимое с JSON форматом значение.
Данный метод не требуется вызывать самому, он вызывается автоматически при вызове <code>json.encode(value)</code>.
END,
}