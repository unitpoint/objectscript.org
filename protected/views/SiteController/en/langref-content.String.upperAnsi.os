return {
	ret = "string",
	retDesc = <<<END'
Новая строка в верхнем регистре. 
END,	
	desc = <<<END"
<p>
Функция возвращает строку, где все символы переведены в верхний регистр. 
<p>
Функция работает с символами в стандарте <b>ANSI</a>.
END,
}