return {
	ret = "Buffer",
	retDesc = <<<END'
Текущий объект.
END,
	desc = <<<END"
Функция преобразует параметры по формату, переданному в первом параметре 
(см. <a href="@url(globals.sprintf)">sprintf</a>), затем добавляет полученную строку
к буферу.
END,
}