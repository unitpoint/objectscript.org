return {
	ret = "mixed",
	retDesc = <<<END'
Значение простого типа.
END,
	desc = <<<END"
Данный метод вызывается автоматически, когда требуется преобразовать экземпляр объекта в простой тип,
например, при конкатенации, а также в функциях 
<a href="@url(globals.toNumber)">toNumber</a>, 
<a href="@url(globals.toString)">toString</a>.
END,
}