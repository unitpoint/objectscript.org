return {
	type = "object",
	desc = <<<END'
<p>
Набор значений, переданных скрипту через переменные окружения.
<p>
Эти значения импортируются в ObjectScript из системных переменных окружения. Большинство значений передаётся 
из командной оболочки, под которой запущен ObjectScript, и различных системных приложений.
END,	
}