return {
	ret = "string",
	retDesc = <<<END'
Новая строка в верхнем регистре. 
END,	
	desc = <<<END"
<p>
Функция возвращает строку, где все символы переведены в верхний регистр. Функция работает 
с национальной кодировкой.
<p>
По умолчанию данная функция установлена в <a href="@url(globals.String.upperAnsi)">upperAnsi</a>.
END,
}