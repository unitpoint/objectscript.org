return {
	// name = 'userdataOf',
	desc = "Функция проверки совместимости значения на пользовательские данные (userdata).",
	ret = 'userdata',
	retDesc = <<<END"
<p>Возвращает результат проверки параметра на пользовательские данные.
<ul>
<li><p>Если тип параметра является пользовательские данные, то его значение будет возвращено.
<li><p>Для др. типов возвращается <code>null</code>.
</ul>
END,			
	params = {
		value = {
			type = 'mixed',
			desc = 'Тестируемое значение',
		},
	},			
}