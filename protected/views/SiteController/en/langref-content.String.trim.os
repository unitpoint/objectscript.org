return {
	ret = "string",
	retDesc = <<<END'
Обрезанная строка.
END,	
	desc = <<<END"
Эта функция возвращает строку с удаленными из начала и конца строки пробельными символами. 
END,
	params = {
		left = {
			type = "boolean",
			def = "true",
			desc = "Обрезать ли пробельные символы с начала строки.",
		},
		right = {
			type = "boolean",
			def = "true",
			desc = "Обрезать ли пробельные символы с конца строки.",
		},
	},
}