return {
	ret = "mixed",
	retDesc = <<<END'
Результат выполнения функции.
END,
	rest = true,
	desc = <<<END"
Вызывает функцию, подставляя указанный объект вместо значения this функции, а остальные параметры вместо аргументов функции.
END,
	params = {
		self = {
			type = "mixed",
			def = "null",
			desc = "Объект для использования в качестве объекта this.",
		},
	},
}