return {
	ret = "mixed",
	retDesc = <<<END'
Удаленное значение из объекта.
END,
	desc = <<<END"
<p>
Удаляет последнее свойство и возвращает его значение.
<p>
Методы <a href="@url(globals.Object.push)">push</a> и <code>pop</code> позволяют моделировать стек типа <b>LIFO</b>.
END,
}