return {
	// name = 'arrayOf',
	desc = "Функция проверки совместимости значения на массив.",
	ret = 'array',
	retDesc = <<<END"
<p>Возвращает результат проверки параметра на массив.
<ul>
<li><p>Если тип параметра является массив, то его значение будет возвращено.
<li><p>Для др. типов возвращается <code>null</code>.
</ul>
END,			
	params = {
		value = {
			type = 'mixed',
			desc = 'Тестируемое значение',
		},
	},			
}