<p>
OS поддерживает восемь типов данных.
<% var c = @controller %>
<p>
Три скалярных типа:
<ul>
<li><p><a href="<%=c.createUrl{params={page="langref.types.bool"}}%>">boolean</a> - <code>true</code> или <code>false</code>
<li><p><a href="<%=c.createUrl{params={page="langref.types.number"}}%>">number</a> - <code>10</code>, <code>0xf0</code>, <code>2.4</code>, <code>5.7e20</code>
<li><p><a href="<%=c.createUrl{params={page="langref.types.string"}}%>">string</a> - <code>"Hello, World!"</code>
</ul>

<p>
Два смешанных типа:

<ul>
<li><p><a href="<%=c.createUrl{params={page="langref.types.array"}}%>">array</a> - <code>[1, 2, 3, "value"]</code>
<li><p><a href="<%=c.createUrl{params={page="langref.types.object"}}%>">object</a> - <code>{name="Ivan", lastname="Petrov"}</code>
</ul>

<p>
Два специальных типа:
<ul>
<li><p><a href="<%=c.createUrl{params={page="langref.types.null"}}%>">null</a> - только одно значение  <code>null</code>
<li><p><a href="<%=c.createUrl{params={page="langref.types.userdata"}}%>">userdata</a> - пользовательские данные для интеграции с кодом на <code>C++</code>
</ul>

<p>
И один тип для функций:
<ul>
<li><p><a href="<%=c.createUrl{params={page="langref.types.function"}}%>">function</a> - <code>function(){ print "Hello, World!" }</code>
</ul>

<p>
Для удобства понимания в этом руководстве используется также несколько псевдотипов:

<ul>
<li><p>mixed - несколько типов данных
<li><p>int - целое число
</ul>

<p>
OS - язык с динамической типизацией, любая переменная может хранить сначало число, потом строку или любой др. тип данных. 
Обычно это определяется во время выполнения программы в зависимости от того, как используется переменная. 
Если же требуется преобразовать один тип в другой, то для этого нужно использовать функции, например, <code>toNumber</code>,
<code>toString</code> и т.п. Для того, чтобы узнать тип переменной, существует функцией <code>typeOf</code>,
которая возвращает строку с названием типа.

<h3>Простые типы</h3>

<p>
К простым типам относятся: 
<a href="<%=c.createUrl{params={page="langref.types.bool"}}%>">boolean</a>,
<a href="<%=c.createUrl{params={page="langref.types.number"}}%>">number</a>,
<a href="<%=c.createUrl{params={page="langref.types.string"}}%>">string</a> и
<a href="<%=c.createUrl{params={page="langref.types.null"}}%>">null</a>. Они так называются, потому что не могут являться
контейнерами для др. значений. Некоторые функции в OS выполняют конвертацию значений автоматически между простыми типами 
при необходимости.

<h3>Сложные типы</h3>

<p>
К сложным типам относятся: 
<a href="<%=c.createUrl{params={page="langref.types.array"}}%>">array</a>,
<a href="<%=c.createUrl{params={page="langref.types.object"}}%>">object</a>,
<a href="<%=c.createUrl{params={page="langref.types.userdata"}}%>">userdata</a> и
<a href="<%=c.createUrl{params={page="langref.types.function"}}%>">function</a>. Значения этих типов могут являться
контейнерами для др. значений.

