return {
	ret = "string",
	retDesc = <<<END'
Новая строка в нижнем регистре. 
END,	
	desc = <<<END"
<p>
Функция возвращает строку, где все символы переведены в нижний регистр. Функция работает 
с национальной кодировкой.
<p>
По умолчанию данная функция установлена в <a href="@url(globals.String.lowerAnsi)">lowerAnsi</a>.
END,
}