<p>
В OS все функции изначально анонимные. Имя функции придает переменная, в которую функция сохранена. 

<p>
В OS анонимную фукнцию можно передавать в параметр др. функции, можно возвращать, можно вызывать, 
не сохраняя ее во временную переменную.

<%@controller.widget('SourceWidget', {source="examples/tutorial.functions.anonymous.osh"})%>
