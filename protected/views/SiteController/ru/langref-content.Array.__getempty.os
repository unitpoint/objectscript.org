return {
	ret = "mixed",
	retDesc = <<<END'
Удаленное значение из объекта.
END,
	desc = <<<END"
Удаляет последний элемент и возвращает его значение (аналог метода <a href="@url(globals.Array.pop)">pop</a>). 
Вызывается в конструкции вида <code>val = arr[]</code>.
END,
}