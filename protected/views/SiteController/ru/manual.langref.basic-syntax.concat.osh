<p>
Конкатенация (или соединение) строк в OS делается оператором <code>..</code> (две точки).
Этот оператор получает два аргумента, которые преобразуются к строковому типу, после чего 
к первому значению присоединяется второе.

<h3>Пример</h3>
<%@controller.widget{'SourceWidget', source="examples/tutorial.operators.concat.osh"}%>
