return {
	ret = "mixed",
	retDesc = <<<END'
Удаленное значение из массива.
END,
	desc = <<<END"
<p>
Удаляет последнее элемент и возвращает его значение.
<p>
Методы <a href="@url(globals.Array.push)">push</a> и <code>pop</code> позволяют моделировать стек типа <b>LIFO</b>.
END,
}