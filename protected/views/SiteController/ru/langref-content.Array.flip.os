return {
	ret = "object",
	retDesc = <<<END'
Возвращает <i>перевернутый</i> массив.
END,
	package = "std.os",
	sourceFile = "https://github.com/unitpoint/objectscript.org/blob/master/framework/components/std.os#L92",
	desc = <<<END"
<p>
Функция возвращает <i>перевернутый</i> массив, то есть значения элементов становятся названиями свойств, 
а индексы - названиями.
<p>
Если значение встречается несколько раз, то в результате сохранится последнее <i>перевернутое</i> значение.
END,
}