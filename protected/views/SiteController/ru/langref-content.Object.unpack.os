return {
	ret = "mixed",
	retDesc = <<<END'
Первые значения свойств из объекта.
END,
	desc = <<<END"
Данный метод должен использоваться с правой стороны от оператора присваивания (<code>=</code>).
Возвращает первые значения свйств из объекта в количестве, необходимом для выполнения операции 
присваивания всем переменным, стоящим с левой стороны от оператора.
END,
}