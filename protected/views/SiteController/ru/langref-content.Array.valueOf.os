return {
	ret = "mixed",
	retDesc = <<<END'
Значение простого типа.
END,
	desc = <<<END"
Данный метод вызывается автоматически, когда требуется преобразовать массив в простой тип,
например, при конкатенации или при вызове <a href="@url(globals.toString)">toString</a>.
END,
}