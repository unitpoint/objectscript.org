return {
	ret = "mixed",
	retDesc = <<<END'
Удаленное значение из объекта.
END,
	desc = <<<END"
Удаляет последнее свойство и возвращает его значение (аналог метода <a href="@url(globals.Object.pop)">pop</a>). 
Вызывается в конструкции вида <code>a = obj[]</code>.
END,
}