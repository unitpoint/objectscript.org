return {
	type = "string",
	desc = <<<END"
<p>
Наименование значения. ObjectScript определяет наименование при первом сохранении значения
в строковое свойство объекта. Таким образом, если значение никуда не сохраненно или сохраненно в локальную переменную
или как элемент масива, то оно не имеет наименования.
END,	
}