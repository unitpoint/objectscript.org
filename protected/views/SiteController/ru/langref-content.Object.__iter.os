return {
	ret = "function",
	retDesc = <<<END'
Функция-итератор.
END,
	desc = <<<END"
Вызывается автоматически для экземпляра объекта в конструкции <code>for in</code> для
обхода свойств объекта.
END,
}