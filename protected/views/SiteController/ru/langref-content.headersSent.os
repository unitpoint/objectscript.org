return 	{
	type = "boolean",
	desc = <<<END'
<p>
Позволяет узнать, были ли отправлены заголовки HTTP.
<p>
Нельзя отправить дополнительные строки заголовков, используя функцию <a href="@url(globals.header)">header</a>, 
если заголовки уже были отправлены. Используя эту функцию, можно предупредить ошибки, связанные с HTTP заголовками. 
Также можно использовать <a href="@url(globals.ob)">Буфферизацию вывода</a>.
END,
}
		